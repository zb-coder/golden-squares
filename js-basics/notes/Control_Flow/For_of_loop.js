// for_of loop

// We use this to iterate over the properties of an object or
// elements in an array.

// THIS IS BETTER FOR ARRAYS

const colors = ['red', 'green', 'blue'];

for (let color of colors)
    console.log(color);

// => red
// => green
// => blue